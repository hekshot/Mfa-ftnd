import Base from "../components/Base";

const About =() =>{
    return (
        <Base>
        <h1>This is our about page</h1>
        <p>we are tryin to build mfa</p>
        </Base>
    );
};

export default About;